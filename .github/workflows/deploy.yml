name: Deploy site to GitHub Pages

on:
  push:
    branches:
      - master

permissions:
  pages: write
  id-token: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PAGES_PAT: ${{ secrets.PAGES_PAT }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Ensure Pages PAT is configured
        if: ${{ env.PAGES_PAT == '' }}
        run: |
          echo "PAGES_PAT is required to enable GitHub Pages automatically."
          echo "Create a repository secret named PAGES_PAT with a PAT that has repo and pages:write permissions."
          exit 1

      - name: Configure Pages
        if: ${{ env.PAGES_PAT != '' }}
        uses: actions/configure-pages@v6
        with:
          token: ${{ env.PAGES_PAT }}
          enablement: true

      - name: Prepare site files
        run: |
          mkdir -p public
          cp index.html android-chrome-192x192.png public/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v5
        with:
          artifact_name: github-pages
